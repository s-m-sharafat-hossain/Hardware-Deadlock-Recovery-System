# Hardware-Deadlock-Recovery-System
This system uses a Python service to monitor OS processes for frozen or deadlocked applications in real time, alerting the user via an Arduino dashboard where a physical push button safely terminates the stuck process.
